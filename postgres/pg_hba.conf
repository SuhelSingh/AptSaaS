# Ubuntu superadmin user (local connection only, requires password)
host    all    ubuntu    127.0.0.1/32    scram-sha-256

# VPN users (automatically authorized based on IP)
host    all    all    10.4.21.1/24    trust

# Deny all other connections
host    all    all    all    reject